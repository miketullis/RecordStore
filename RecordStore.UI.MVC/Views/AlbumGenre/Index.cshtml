@model IEnumerable<RecordStore.Data.EF.AlbumGenre>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Album.AlbumName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Genre.GenreName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Album.AlbumName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Genre.GenreName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AlbumGenreID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AlbumGenreID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AlbumGenreID })
        </td>
    </tr>
}

</table>
