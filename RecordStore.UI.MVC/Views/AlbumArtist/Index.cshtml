@model IEnumerable<RecordStore.Data.EF.AlbumArtist>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Album.AlbumName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Artist.ArtistName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Album.AlbumName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Artist.ArtistName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AlbumArtistID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AlbumArtistID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AlbumArtistID })
        </td>
    </tr>
}

</table>
